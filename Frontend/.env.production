# Production environment overrides used by the CRA build.
# REACT_APP_API_URL is set in the hosting provider (Vercel) — do not commit a value here.

GENERATE_SOURCEMAP=false
INLINE_RUNTIME_CHUNK=false
